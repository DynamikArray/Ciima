{
  "name": "ciima",
  "version": "1.0.0",
  "description": "Comic Issue Inventory Management Application",
  "main": "server/server.js",
  "scripts": {
    "dev": "concurrently \" nodemon server/server.js \" \"cd client && npm run serve \" ",
    "start:server": "set NODE_ENV=development &&  node server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd client;  npm install --dev ; npm run serve",
    "start": "node server/server.js"
  },
  "author": "Brian Ridsdale",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.1.0",
    "fastify": "^2.8.0",
    "fastify-cors": "^2.1.3",
    "fastify-mysql": "^0.3.0",
    "fastify-static": "^2.5.0",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "concurrently": "^4.1.2"
  }
}
